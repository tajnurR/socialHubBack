# Development profile: verbose SQL + debug logging, local-friendly defaults.
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.socialhub: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
