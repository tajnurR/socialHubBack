package com.socialhub.socialhubBackend.integration.core;

/**
 * Supported social platforms. Add a new constant here and a matching
 * {@code SocialMediaProvider} implementation in its own package — nothing else
 * in the codebase needs to change.
 */
public enum SocialPlatform {
    FACEBOOK,
    INSTAGRAM,
    WHATSAPP,
    TIKTOK,
    YOUTUBE,
    LINKEDIN,
    X,
    PINTEREST
}
